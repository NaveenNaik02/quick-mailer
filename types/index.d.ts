type ShowToastErrorProps = {
  variant?: "default" | "destructive";
  title?: string;
  description?: string;
};

type ShowToastSuccess = {
  variant?: "default" | "destructive";
  title?: string;
  description: string;
};
